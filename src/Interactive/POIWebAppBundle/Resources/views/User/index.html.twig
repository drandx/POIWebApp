{% extends 'POIWebAppBundle:Administrator:dashboard.html.twig' %}
{% set menu_selected = 'three' %}
{% block content %}
<h2 class="sub-header">Usuarios Administradores</h2>

<div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th><a href="#">Id</th>
                <th><a href="#">Nombre</th>
                <th><a href="#">Apellido</th>
                <th><a href="#">Correo Electrónico</th>
                <th><a href="#">Acciones</th>
            </tr>
        </thead>
        <tbody>
            {% for entity in entities %}
            <tr>
                <td><a href="{{ path('poi_user_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.firstName }}</td>
                <td>{{ entity.lastName }}</td>
                <td>{{ entity.email }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('poi_user_edit', { 'id': entity.id }) }}">editar</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
<ul>
    <li>
        <a href="{{ path('poi_user_new') }}">
            Crear Usuario
        </a>
    </li>
</ul>
    {% endblock %}
